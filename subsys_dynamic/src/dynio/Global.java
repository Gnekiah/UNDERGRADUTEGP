package dynio;

public class Global {
    
    public final static boolean DEBUG = true;
    // do test in this path including compile and run
    public final static String DO_TEST_DIR = "./dotest/";
    // listening the port for reveiving files
    public final static int LISTENING_PORT = 50022;
    // accept buffer
    public final static int ACCEPT_BUFFER_SIZE = 65534; 
    // send buffer
    public final static int SEND_BUFFER_SIZE = 65534;
    // error info buffer
    public final static int ERRINFO_BUFFER_SIZE = 65534;
    // sleep in milliseconds
    public final static int DO_SLEEP = 1000;
    // monitor path
    public final static String MONITOR_PATH = "./monitor";
    
    
}
